# Database configuration - for XAMPP MySQL
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/testcasedb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}

# Server configuration
server.port=8080

# JWT configuration - now read from environment variables
tcm.app.jwtSecret=${JWT_SECRET:nfRYac4ckqPUyRbrVQQbkVADJbV2fxKNYIXNAPOso0Y=}
tcm.app.jwtExpirationMs=604800000

# Default admin user configuration - now read from environment variables
tcm.app.defaultAdminUsername=${ADMIN_USERNAME:admin}
tcm.app.defaultAdminPassword=${ADMIN_PASSWORD:secureAdminPass2024!}
tcm.app.defaultAdminEmail=${ADMIN_EMAIL:admin@test.com}

# Logging configuration
logging.level.com.yourproject.tcm.security=DEBUG
